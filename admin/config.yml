backend:
  name: github
  repo: your-github-username/your-repo-name
  branch: main
  base_url: https://decap-oauth.verifitrust.workers.dev  # Use your actual Worker URL here
  auth_endpoint: auth

media_folder: "assets/images"
public_folder: "/assets/images"

collections:
  # BLOG POSTS
  - name: "blog"
    label: "Blog Posts"
    folder: "blog/"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Body", name: "body", widget: "markdown"}

  # CASE STUDIES
  - name: "case-studies"
    label: "Case Studies"
    folder: "case-studies/"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Company Name", name: "title", widget: "string"}
      - {label: "Summary", name: "summary", widget: "text"}
      - {label: "Body", name: "body", widget: "markdown"}
